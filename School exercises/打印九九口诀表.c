#include <stdio.h>

int main() {
    // 原始代码（完整保留用于对比）
    /*
    int n;
    scanf("%d", &n);
    char* arr[] = { "1*1=1\t\n",
                    "1*2=2   2=4\t\n",   // 错误1：字符串"2=4"应为"2*2=4"
                    "1*3=3   2*3=6   3*3=9\t\n"   // 错误2：元素间缺少逗号
                    "1*4=4   2*4=8   3*4=12  4*4=16\t\n"  // 错误2：元素间缺少逗号
                    "1*5=5   2*5=10  3*5=15  4*5=20  5*5=25\t\n"  // 错误2：元素间缺少逗号
                    "1*6=6   2*6=12  3*6=18  4*6=24  5*6=30  6*6=36\t\n"  // 错误2：元素间缺少逗号
                    "1*7=7   2*7=14  3*7=21  4*7=28  5*7=35  6*7=42  7*7=49\t\n"  // 错误2：元素间缺少逗号
                    "1*8=8   2*8=16  3*8=24  4*8=32  5*8=40  6*8=48  7*8=56  8*8=64\t\n"  // 错误2：元素间缺少逗号
                    "1*9=9   2*9=18  3*9=27  4*9=36  5*9=45  6*9=54  7*9=63  8*9=72  9*9=81\t\n"  
                    };
    for(int i = 0; i < 8; i++) {  // 错误3：循环条件i<8，最多输出8行，不支持n=9
        if (i <= n-1)
            printf("%c", arr[i]);  // 错误4：用%c输出字符串（应为%s）
    } 
    return 0;	
    */

    // 优化后代码
    int n;
    // 1. 鲁棒性优化：输入检查（覆盖非整数、范围外输入）
    if (scanf("%d", &n) != 1 || n < 1 || n > 9) {
        printf("输入错误：请输入1-9之间的正整数\n");
        return 1;
    }

    // 2. 动态生成法：避免硬编码，支持任意1-9的n
    // 外层循环控制行数（1到n）
    for (int row = 1; row <= n; row++) {
        // 内层循环控制每行的列数（1到当前行号，保证下三角结构）
        for (int col = 1; col <= row; col++) {
            // 3. 格式优化：等号右边数字占4位、左对齐（%-4d）
            printf("%d*%d=%-4d", col, row, col * row);
        }
        printf("\n");  // 每行结束换行
    }

    return 0;
}

/*
原始代码与优化代码对比分析：
1. 语法错误修复：
   - 原始：字符串数组元素间缺少逗号，导致编译失败；"2=4"存在语法错误（漏写*2）
   - 优化：移除硬编码字符串，用动态生成避免语法错误

2. 输出逻辑修复：
   - 原始：用%c输出字符串（%c用于单个字符），循环条件i<8限制最大行数为8
   - 优化：用%s的替代方案（动态生成），循环条件row<=n支持1-9所有行数

3. 格式合规性：
   - 原始：硬编码空格，无法保证"等号右边占4位左对齐"（如"1*2=2   "与"3*4=12 "格式不一致）
   - 优化：用%-4d精确控制格式，所有结果统一左对齐占4位

4. 灵活性对比：
   - 原始：修改n需手动增删字符串数组元素，易出错
   - 优化：动态计算输出，n变化时无需修改代码，适配所有1-9的输入

5. 复杂度对比：
   - 原始：空间复杂度O(9)（存储9个字符串），时间复杂度O(n)
   - 优化：空间复杂度O(1)（无额外存储），时间复杂度O(n2)（n≤9时运算量可忽略）
*/

