/*思路分析：
N名同学
姓名 <= 8 字符串
语文、数学、英语 < 150
每科成绩分差 <= 5
sum <= 10
"旗鼓相当的对手"
两两比较*/

#include <stdio.h>
#include <stdlib.h>


typedef struct student {
	char* name+;
	int chinese;
	int math;
	int english;
	
}student;

int main() {
	int n;
	scanf("%d", &n);
	student Student[n];
	for(int i = 0; i < n; i++) {
		Student[i] = {,,}
	}
	
} 
